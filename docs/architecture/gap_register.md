# Gap Register

| Gap | Current state | Target state | Phase | Validation signal |
|---|---|---|---|---|
| RTR optimizer calibration | Issue `#27` lands the internal-model-only RTR optimizer with seed crop-specific weights, bounds, and risk penalties, but those values are still starter defaults rather than grower-approved house calibration | Crop-specific weights, bounds, and risk tolerances are tuned against house history and operator review without regressing the landed optimizer contracts | Post-PR calibration | House-side replay comparisons, grower review, and updated optimizer regression fixtures |
| RTR good-production windows | `configs/rtr_good_windows.yaml` still reflects demo or heuristic windows rather than grower-approved good-production periods | Grower-approved windows drive the baseline-prior recalibration path, and `configs/rtr_profiles.json` is regenerated from those windows | Post-PR calibration | `poetry run python scripts/calibrate_rtr.py --windows configs/rtr_good_windows.yaml --output configs/rtr_profiles.json` plus reviewer sign-off on the selected windows |
| RTR actuator-depth follow-up | The landed optimizer computes day/night minimum temperatures, mean target, vent bias, and screen bias, but does not yet drive a full control-sequence MPC or actuator execution lane | A later control-depth lane can extend the bounded day/night optimizer into finer step sequencing without breaking the current additive RTR contracts | Separate follow-up | Additional optimizer/service ADR plus simulation-regression coverage for stepwise setpoint sequences |
| RTR area-settings persistence promotion | Issues `#27`, `#32`, and `#34` now add additive `/api/rtr/area-settings`, hydration/persistence guards, and explicit source labeling for default/server/local area projections, but greenhouse/user defaults are not yet promoted into the long-term house configuration path | Actual-area defaults are persisted per greenhouse/user in the canonical config path while preserving the current local override UX | Post-PR calibration | Backend persistence tests and frontend hydration tests over greenhouse-scoped defaults |
| Crop-model service layer | Adapter-backed `crop_models/` wrappers now expose canonical cucumber and tomato state payloads plus bounded event application, but reusable physiology helpers are still mostly consumed through orchestration seams instead of a cleaner service split | Explicit cucumber and tomato services own state, events, and reusable physiology helpers end-to-end | Phase 4 | Unit tests for state payloads, legacy-wrapper contracts, and cleaner service ownership |
| Advisor API service split | Exact additive `/api/advisor/environment`, `/api/advisor/physiology`, `/api/advisor/work-tradeoff`, `/api/advisor/harvest`, and `/api/advisor/chat` routes exist, but the underlying service split is still orchestrator-centric | New model-backed advisor endpoints retain current contracts while moving more logic into dedicated service modules | Phase 4 | Route contract tests plus orchestration/service ownership review |
| Retrieval role reduction | Retrieval is still a visible part of the landed SmartGrow baseline even though model-runtime outputs now drive the mainline decisions | Retrieval remains explanation-only and clearly subordinate to model/runtime outputs across the dashboard and advisor lanes | Cross-cutting | Architecture docs, orchestration tests, and UI provenance guards |
