# Gap Register

| Gap | Current state | Target state | Phase | Validation signal |
|---|---|---|---|---|
| Harness truth realignment | Issue `#18` docs and `.rah` state previously pointed to knowledge/RAG and nutrient-calculator follow-up work | Issue `#19` now owns the model-first mainline and issue `#18` is preserved as baseline | Phase 0 | Blueprint, ADR, module spec, and `.rah` state all agree on issue `#19` |
| Crop-model service layer | Adapter-backed `crop_models/` wrappers now expose canonical cucumber and tomato state payloads plus bounded event application, but reusable physiology helpers are still missing | Explicit cucumber and tomato services own state, events, and reusable physiology helpers | Phase 1 | Unit tests for state payloads and legacy-wrapper contracts |
| Work-event state store | A dedicated SQLite sidecar now records `crop_model_states`, `crop_model_snapshots`, and `crop_work_events` with before/after linkage, but only the bounded phase-1 event set is implemented | `crop_model_states`, `crop_model_snapshots`, and `crop_work_events` record before/after state and confidence-aware work events | Phase 1 | Event-schema tests plus snapshot replay tests |
| Gas exchange and canopy integration | Reusable FvCB/Ball-Berry/canopy services now feed normalized cucumber/tomato snapshots, but they are not yet consumed by the advisor-contract layer | Dedicated gas-exchange, stomatal, and canopy services feed model runtime and advisor layers | Phase 2 | Stability tests under hot/low-light/low-CO2/low-humidity conditions |
| Scenario and sensitivity runtime | Additive `/api/models/scenario` and `/api/models/sensitivity` now expose bounded scenarios, local finite-difference sensitivities, trust-region checks, and persisted runtime outputs, but advisor contracts still do not consume them | Bounded scenarios, local sensitivities, and constraints drive recommendation ranking | Phase 2 | Finite-difference sign tests, trust-region tests, and scenario-direction consistency tests |
| Advisor API migration | Current `/api/advisor/*`, `/api/environment/recommend`, and `/api/work/recommend` now preserve the issue `#18` compatibility fields while additively attaching a model-first `machine_payload.model_runtime` block built from the landed bounded scenario/sensitivity runtime | New model-backed advisor endpoints serve physiology, environment, work-tradeoff, harvest, and assistant flows | Phase 3 | Route contract tests plus orchestration regression tests |
| Model-aware frontend surfaces | The `environment`, `physiology`, `work`, and `harvest_market` tabs render model-runtime UI, Assistant chat replies keep a compact runtime strip, and the dashboard-level `AiAdvisor` summary card now consumes `/api/advisor/summary` through a compact runtime surface; broader compare/event-driven polish is still a follow-up | Existing advisor shells are re-fed by model-backed payloads with action cards, compare UI, and confidence/missing-data handling across every advisor consumer | Phase 3 | Frontend lint/build plus targeted UI contract tests |
| Retrieval role reduction | Retrieval is currently a large part of the landed SmartGrow story | Retrieval remains useful but is explicitly subordinate to model state and explanation-building | Cross-cutting | Architecture docs, orchestration tests, and UI provenance guards |
| RTR good-production windows follow-up | Issue `#3` remains blocked on grower-approved periods | Grower-approved windows replace the demo segments and `configs/rtr_profiles.json` is regenerated | Separate follow-up | `poetry run python scripts/calibrate_rtr.py --windows configs/rtr_good_windows.yaml --output configs/rtr_profiles.json` |
