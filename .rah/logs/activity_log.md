# Activity Log

- 2026-04-10: Created issue `#88` with branch `docs/88-refresh-post-issue84-blueprint-baseline-and-backlog-pointers`, found that `Phytoritas.md`, `docs/architecture/Phytoritas.md`, and the top-level `.rah` restart gates still pointed at the older post-issue74 / issue76 baseline, refreshed the root/mirror blueprint pointers and restart packets to the post-issue84 merged truth, recorded that open issue `#3` remains intentionally blocked on real grower-approved RTR windows, reran the skill-path `automation/rah.py doctor|status|resume`, and revalidated with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, `npm --prefix frontend run build`, `poetry run ruff check .`, `poetry run pytest`, and `git diff --check`.
- 2026-04-10: Pushed follow-up commit `63b530b` to PR `#85`, changed `frontend/src/hooks/useRtrOptimizer.ts` so `/control` publishes `/rtr/optimize` results before slower scenario/sensitivity responses finish, added deferred-response coverage in `frontend/src/hooks/useRtrOptimizer.test.tsx`, revalidated with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, `npm --prefix frontend run build`, `poetry run ruff check .`, `poetry run pytest`, and `git diff --check`, and captured the refreshed `/control` evidence at `artifacts/screenshots/issue84-control-early-hydration.png`.
- 2026-04-10: Confirmed PR `#85` GitHub Actions Backend/Frontend validation both passed, merged the PR into `main` at `64400a8`, confirmed issue `#84` closed, and opened issue `#86` on branch `docs/86-sync-issue84-merged-harness-state-on-main` to realign `.rah` around the post-issue84 merged baseline on `main`.
- 2026-04-10: Opened issue `#84` on branch `fix/84-fix-control-realtime-failures-and-user-facing-fetch-errors`, changed `frontend/src/config.ts` so the frontend heals back to `127.0.0.1:8000` instead of persisting stale standard-local `8003` overrides, added `frontend/src/utils/requestErrorCopy.ts` plus unit coverage for localized network/404/5xx copy, rewired the control RTR surfaces to show grower-facing Korean fallback text instead of raw `Failed to fetch` / `Not Found`, revalidated with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, `npm --prefix frontend run build`, `poetry run ruff check .`, `poetry run pytest`, and `git diff --check`, and captured the recovered `/control` screen at `artifacts/screenshots/issue84-control-realtime-recovered.png`.
- 2026-04-10: Merged PR `#70` into `main` at `1853af6`, confirmed issue `#69` closed with project status `Done`, fast-forwarded local `main`, opened issue `#72` on branch `docs/72-sync-issue69-merged-harness-state-on-main`, closed duplicate issue `#71`, and reset `.rah` back to the post-issue69 merged baseline so the next restart uses truthful `main` identities instead of the retired issue69 branch.
- 2026-04-10: Created issue `#69` with branch `fix/69-clean-remaining-control-copy-residue-after-compact-shell`, localized the remaining `/control` route and outlook wording in `route-meta.ts`, `control-page.tsx`, `control-route-page.tsx`, `RTROutlookPanel.tsx`, and `WeatherOutlookPanel.tsx`, carried the same weather-label localization into `App.tsx` and `DecisionSnapshotGrid.tsx`, replaced the Korean weather card's raw backend English summary with a local Korean operating summary, revalidated with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, `npm --prefix frontend run build`, `poetry run ruff check .`, and `poetry run pytest`, and captured the refreshed `/control` screen at `artifacts/screenshots/issue69-control-copy-cleanup-final.png`.
- 2026-04-09: Continued issue `#61` routed-shell cleanup by removing the dead advisor-only `leftColumnSurface` compatibility composition from `frontend/src/App.tsx`, deleting the orphaned `advisorTabsAnchorRef` scroll wiring, and revalidating with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, and `npm --prefix frontend run build` (`19 files, 61 passed`).
- 2026-04-09: Continued issue `#61` routed-shell cleanup by gating `legacyRouteFrameProps` to `/overview|/control|/resources|/alerts` legacy paths only, adding `/overview/legacy` direct-entry coverage against the mocked `MainDashboard` compatibility frame in `frontend/src/App.routing.test.tsx`, and revalidating with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, and `npm --prefix frontend run build` (`19 files, 62 passed`).
- 2026-04-09: Confirmed PR `#58` GitHub Actions Backend/Frontend validation both passed (runs `24188663087` and `24188664697`), merged the PR into `main` at `1d0c24ff432aec8403b9a9e2e1673bc5752c8889`, verified issue `#57` closed with project status `Done`, and opened issue `#59` to sync the root `.rah` control-plane back to merged truth.
- 2026-04-09: Committed the issue `#57` chunk-splitting slice as `f014514` (`fix: reduce frontend entry chunk size warning`), pushed `fix/57-reduce-frontend-entry-chunk-size-with-route-level-code-splitting`, opened PR `#58`, and moved the project item to `Validating` while GitHub Actions Backend/Frontend validation started on the latest head.
- 2026-04-09: Created issue `#57` with branch `fix/57-reduce-frontend-entry-chunk-size-with-route-level-code-splitting`, split React/router/lucide vendor groups in `frontend/vite.config.ts`, reduced the production `index` chunk from `513.46 kB` to `284.08 kB` so the Vite `>500 kB` warning disappeared, and revalidated locally with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, and `npm --prefix frontend run build`.
- 2026-04-09: Created issue `#53` with branch `fix/53-retune-remaining-dashboard-surfaces-to-warm-editorial-palette`, retuned the remaining visible dashboard shell, advisor, RTR, analytics, market, crop detail, and knowledge drawer surfaces to the warm editorial palette, revalidated locally with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, and `npm --prefix frontend run build`, then merged PR `#54` into `main` at `c8febc2e3a2b3df0c11d2d19e1a5127194c83b99` after both GitHub Actions validation lanes passed.
- 2026-04-09: Created issue `#51` with branch `docs/51-sync-root-harness-state-after-issue49-merge-cleanup` to resynchronize the root `.rah` control-plane after the merged issue `#49` warm-editorial UI follow-up and the local worktree cleanup pass.
- 2026-04-09: Committed issue `#49` as `699a95d` (`feat: finish warm editorial phytosync ui follow-up`), pushed `feat/49-implement-remaining-phytosync-redesign-prompt-gaps-on-mainline`, opened PR `#50`, and confirmed the branch was clean after rerunning `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, `npm --prefix frontend run build`, and `git diff --check`.
- 2026-04-09: Confirmed PR `#50` GitHub Actions Backend/Frontend validation both passed (push run `24184534147`, pull_request run `24184543933`), merged the PR into `main` at `553c36df422416b9001b19dbb43f8a52fa6ef8d3`, and verified issue `#49` closed with project status `Done`.
- 2026-04-09: Normalized the root repository after issue `#49` by restoring tracked dirty files, moving local-only screenshots/data/stitch references into `.git/info/exclude`, stopping lingering `vite`/`esbuild`/`uvicorn` processes that blocked deletion, removing obsolete issue43/45/47/49 worktree folders, and fast-forwarding root `main` to the merged PR `#50` head.
- 2026-04-08: Created issue `#41` with branch `feat/41-introduce-actuator-first-post-control-rtr-seam`, promoted the live RTR path from temperature-first guidance into an actuator-first control seam with first-class heating/cooling/ventilation/thermal-screen/circulation-fan/CO2 candidates, added `actuator_bridge`, `control_effects`, `post_control_state`, `crop_bridge`, and `labor_estimator`, rewired the optimizer/scenario/sensitivity stack plus `RTROptimizerPanel` around post-control microclimate evaluation, refreshed README and the RTR architecture note, and validated locally with `npm --prefix frontend run lint`, `npm --prefix frontend run test`, `npm --prefix frontend run build`, `poetry run ruff check .`, and `poetry run pytest`.
- 2026-04-08: Committed issue `#41` feature work as `88d21ca` (`feat: introduce actuator-first post-control RTR seam`), pushed `feat/41-introduce-actuator-first-post-control-rtr-seam`, opened PR `#42`, and moved the project item for issue `#41` to `Validating`.
- 2026-04-08: Fixed the reviewer-blocking findings on issue `#41` by removing ventilation-energy double counting, aligning optimize/scenario baseline semantics through a shared baseline control candidate, making sensitivity objective weights honor `include_energy_cost` and `include_labor_cost` even in `custom_weights` mode, preserving explicit zero-valued circulation-fan custom overrides, and revalidating with the full local ladder (`npm --prefix frontend run lint`, `npm --prefix frontend run test`, `npm --prefix frontend run build`, `poetry run ruff check .`, `poetry run pytest`, `git diff --check`) for a final `144 passed, 31 warnings`.
- 2026-04-08: Committed the issue `#41` blocker fix-up as `75d95c2` (`fix: align actuator-first RTR baseline and energy accounting`) and pushed it to PR `#42`, so remote Backend/Frontend validation now needs to be checked again on the refreshed head.
- 2026-04-08: Merged PR `#38` into `main` at `d7861f8`, confirmed issue `#37` closed with project status `Done`, and reset the harness runtime state back to a no-active-loop merged baseline while keeping issue `#3` as the optional content-calibration follow-up for real grower-approved windows.
- 2026-04-08: Created issue `#37` with branch `feat/37-add-rtr-calibration-workspace-for-user-entered-grower-windows-and-house-tuning` after clarifying that real grower-approved windows are calibration content rather than an RTR implementation blocker, then added additive `/api/rtr/calibration-state|preview|save`, house-scoped grower-window normalization/persistence helpers, a new `RTRCalibrationWorkspace` surface inside `RTROptimizerPanel`, and hook/component/backend regression coverage while keeping the full local validation ladder green (`npm --prefix frontend run test`, `npm --prefix frontend run lint`, `npm --prefix frontend run build`, `poetry run ruff check .`, `poetry run pytest`).
- 2026-04-08: Committed issue `#37` as `eaa92ef` (`feat: add RTR calibration workspace`), pushed `feat/37-add-rtr-calibration-workspace-for-user-entered-grower-windows-and-house-tuning`, opened PR `#38`, and moved the project item for issue `#37` to `Validating`.
- 2026-04-08: Committed issue `#32` as `a906d58` (`fix: harden rtr optimizer loading gates`), pushed `feat/32-harden-rtr-optimizer-frontend-contract-and-loading-gates`, opened PR `#33`, and moved the project item for issue `#32` to `Validating`.
- 2026-04-08: Created issue `#32` and branch `feat/32-harden-rtr-optimizer-frontend-contract-and-loading-gates`, then hardened the RTR optimizer frontend so profile loading no longer triggers optimistic enabled/balanced requests, node-target hydration must come from `/api/rtr/state` or explicit user input before optimizer calls fire, localStorage-restored local area overrides are not re-persisted on first mount, raw `/api/rtr/profiles` payload fields are preserved in the hook, and the full local ladder stayed green with `npm --prefix frontend run test`, `npm --prefix frontend run lint`, `npm --prefix frontend run build`, `poetry run ruff check .`, `poetry run pytest tests/test_rtr_optimizer.py`, `poetry run pytest tests/test_smoke.py -k rtr`, and full `poetry run pytest`.
- 2026-04-08: Hardened the pesticide advisor grower-detail slice on issue `#23` by localizing product-name preference and rotation/cycle/backup-option wording in the frontend, switching backend pesticide detail payloads to locale-neutral additive fields, adding legacy-payload and English-locale regression coverage, and validating the refreshed PR `#24` head `ed9fd2a` with the full local ladder plus green GitHub Actions Backend/Frontend validation.
- 2026-04-02: Verified repo-local AGENTS workflow and created issue `#1` with branch `feat/1-refactor-current-repo-around-dashboard-eng-1-1-architecture`.
- 2026-04-02: Bootstrapped `docs/architecture/` and `.rah/` for the source-project migration loop.
- 2026-04-02: Migrated the first backend runtime slice into `src/model_informed_greenhouse_dashboard/` with package-safe imports and repo-root config loading.
- 2026-04-02: Validated the backend slice with `poetry run pytest` and `poetry run ruff check .`.
- 2026-04-02: Fixed the canonical UI migration path to the source `frontend/` Vite app.
- 2026-04-02: Migrated the source `frontend/` Vite workspace into `frontend/` and copied the source CSV fixtures into `data/`.
- 2026-04-02: Hardened crop-scoped config/control endpoints so frontend flows can queue or apply per-crop state safely.
- 2026-04-02: Validated the migrated UI with frontend typecheck, frontend lint, and frontend production build.
- 2026-04-02: Passed a live browser smoke on `127.0.0.1` backend/frontend, including crop switching, tomato config update, cucumber prune, and a saved screenshot under `artifacts/browser-smoke/`.
- 2026-04-02: Split the frontend into lazy and vendor chunks, removed the Vite `>500 kB` warning, and passed a built-preview browser smoke with saved screenshot and network log under `artifacts/browser-smoke/`.
- 2026-04-03: Switched the backend AI helper from Gemini to the OpenAI Responses API, added `OPENAI_API_KEY` loading from the repo-root `.env`, and validated the new helper with unit tests.
- 2026-04-03: Diagnosed the live AI failure as a repo-root `.env` key that loads correctly but is rejected by OpenAI with `401 invalid_api_key`, then hardened the service to degrade gracefully with an explicit invalid-key message.
- 2026-04-03: Added source-style launcher scripts (`start_all.bat`, `scripts/start_all.bat`, `scripts/start_all.sh`) so backend/frontend can be started without manually typing the Poetry and npm commands.
- 2026-04-03: Hardened the launcher scripts so switching between Windows and WSL/Git Bash auto-repairs the current platform's Rollup native dependency instead of failing on the cross-platform optional-dependency mismatch.
- 2026-04-03: Updated the README launcher examples to show the required PowerShell `.\start_all.bat` form alongside the bash equivalents.
- 2026-04-03: Fixed crop-scoped frontend graph binding, removed duplicate backend sim broadcasts, normalized dashboard tile labels and full-unit copy with CO₂/H₂O superscript and subscript rendering, and passed lint/test/build plus a live browser smoke with screenshots.
- 2026-04-03: Confirmed that the repo-root `.env` key is valid while a revoked global `OPENAI_API_KEY` was overriding it in running shells/processes, then changed backend config loading so repo-local `.env` wins for `OPENAI_API_KEY` and `OPENAI_MODEL`.
- 2026-04-03: Simplified frontend unit copy back to compact unit notation only (`°C`, `ppm`, `kPa`, `kg`, `kWh`, etc.) and removed the previously added spelled-out English unit phrases from dashboard displays.
- 2026-04-03: Traced the 'stomatal conductance is always 0' complaint to low-magnitude canopy values from the early cucumber replay window plus one-decimal frontend formatting, then switched dashboard metric rendering to adaptive precision and applied the same small-value formatting to the transpiration detail tile.
- 2026-04-03: Fixed AI dashboard context assembly so consult/chat fall back to the current sensor snapshot when history is still warming up, and delayed auto-analysis until real crop data exists instead of firing once against an empty `recentSummary`.
- 2026-04-03: Reduced frontend render pressure by batching websocket-driven state commits to 250 ms windows, wrapping expensive chart panels in `memo`, and feeding them deferred history/metric props instead of forcing full-rate redraws on every streamed step.
- 2026-04-03: Added a live Daegu weather outlook panel backed by an Open-Meteo-powered backend endpoint, showing current conditions plus a 3-day forecast summary independently of the historical greenhouse replay stream.
- 2026-04-03: Hardened runtime refresh behavior so the launcher clears stale listeners on ports `8000/5173`, `/api/status` marks exhausted replays as `completed`, and the frontend auto-restarts crop simulations instead of staying frozen at an already-finished replay.
- 2026-04-03: Refreshed `baseline-browser-mapping`, re-ran lint/build without the old tooling notice, and passed a live browser smoke on temporary ports showing the Daegu weather panel loaded successfully while cucumber replay timestamps continued advancing.
- 2026-04-03: Preserved the live Daegu weather panel and extended the same right-side dashboard zone with a neighboring rolling-24h RTR monitor, adopting a WUR/TomatoesNZ-style example strategy line and 3-day forecast-linked steering targets for tomato and cucumber operations.
- 2026-04-03: Revalidated the RTR extension with `poetry run ruff check .`, `poetry run pytest`, `npm run lint`, `npm run build`, and a browser smoke on `127.0.0.1:8010` + `127.0.0.1:4174`; the screenshot was saved to `artifacts/browser-smoke/rtr-panel-extension.png`.
- 2026-04-03: Externalized RTR coefficients into `configs/rtr_profiles.json`, added `/api/rtr/profiles`, wired the frontend RTR panel to runtime profiles with baseline fallback, and added `scripts/calibrate_rtr.py` plus backend fitting utilities for future house-specific calibration.
- 2026-04-03: Verified the RTR calibration pipeline with `poetry run python scripts/calibrate_rtr.py --output artifacts/rtr-calibration/test_rtr_profiles.json`, then passed a browser smoke on `127.0.0.1:8010` + `127.0.0.1:4174` that confirmed the dashboard now reads `/api/rtr/profiles`; the screenshot was saved to `artifacts/browser-smoke/rtr-calibration-pipeline.png`.
- 2026-04-03: Added the curated good-production window input contract at `configs/rtr_good_windows.yaml`, taught the RTR calibration service and CLI to prefer those windows before heuristic fallback, and revalidated the pipeline with unit tests plus a dry-run calibration that still reports `heuristic-fallback` until real windows are enabled.
- 2026-04-03: Replaced the placeholder RTR windows with concept-demo high-fit segments from the sample tomato/cucumber environment histories, regenerated `configs/rtr_profiles.json`, and recorded the rationale in `docs/architecture/implementation/rtr_calibration_window_selection.md`.
- 2026-04-03: Threaded weather and RTR profile/live/forecast summaries into the AI consult/chat dashboard JSON, documented the new prompt fields in the OpenAI service, and passed a temp-port browser smoke whose screenshot was saved to `artifacts/browser-smoke/rtr-curated-house-fit.png`.
- 2026-04-03: Tightened the OpenAI prompt contract so weather/RTR are treated as required live context when present, then validated direct live consult/chat calls against the OpenAI API; both outputs explicitly referenced Daegu weather plus RTR target/balance information.
- 2026-04-03: Added `.github/workflows/ci.yml` so GitHub Actions now mirrors the local validation ladder with backend Poetry checks and frontend npm lint/build, then updated the harness state to make remote CI / PR sync the next GitHub workflow step.
- 2026-04-03: Synced the GitHub project workflow by moving issue `#1` to `Validating` with `scripts/Set-GitHubProjectStatus.ps1` and adding a progress comment on the issue that links the current local validation state to the pending remote CI run after push.
- 2026-04-03: Re-ran `npm --prefix frontend run build`, `npm --prefix frontend run lint`, `poetry run pytest`, and `poetry run ruff check .`, then refreshed stale blueprint/gap/doctor artifacts so the harness resume surfaces match the current validated local state before commit/push.
- 2026-04-03: Committed and pushed the migrated dashboard runtime plus harness scaffold to `feat/1-refactor-current-repo-around-dashboard-eng-1-1-architecture`, opened PR `#2` with the repo helper script, and confirmed both push + pull_request GitHub Actions CI runs passed while issue `#1` stayed in `Validating`.
- 2026-04-03: Re-smoked representative live OpenAI consult/chat calls with the repo-local `.env` key after PR/CI setup; both calls succeeded and used the live weather/RTR context in the generated advice.
- 2026-04-03: Created issue `#6` and branch `data/6-add-live-produce-price-panel-from-kamis-api` for a new KAMIS-backed live produce price panel loop instead of overloading the blocked RTR follow-up issue `#3`.
- 2026-04-03: Added a cached `/api/market/produce` KAMIS integration, a new frontend polling hook and Live Produce Prices panel, and local env examples for optional KAMIS credential override while retaining the official docs sample auth fallback.
- 2026-04-03: Validated issue `#6` locally with `npm --prefix frontend run lint`, `poetry run ruff check .`, `poetry run pytest`, `npm --prefix frontend run build`, a direct live KAMIS fetch smoke, and a browser smoke on `127.0.0.1:8000/4173`; artifacts were saved under `artifacts/browser-smoke/issue6-produce-price-panel.png` and `artifacts/browser-smoke/issue6-produce-price-console.log`.
- 2026-04-03: Pushed commit `dde1a0d`, opened PR `#7` for issue `#6`, and confirmed both push + pull_request GitHub Actions CI runs passed while the project item moved to `Validating`.
- 2026-04-05: Created issue `#8` and branch `data/8-add-2-week-produce-price-trends-and-seasonal-normals` to extend the KAMIS panel with a trailing 14-day actual trend plus forward 14-day 3y/5y/10y seasonal-normal overlays.
- 2026-04-05: Added KAMIS `periodRetailProductList` trend aggregation to `/api/market/produce`, including per-featured-item history windows, 3-year/5-year/10-year seasonal averaging, and frontend trend-chart rendering while preserving the existing live price cards.
- 2026-04-05: Validated issue `#8` locally with `npm --prefix frontend run lint`, `poetry run ruff check .`, `poetry run pytest`, `npm --prefix frontend run build`, a direct live KAMIS series fetch smoke, and a browser smoke on `127.0.0.1:8012/4175`; artifacts were saved under `artifacts/browser-smoke/issue8-produce-trend-panel.png`, `artifacts/browser-smoke/issue8-console.log`, `artifacts/browser-smoke/issue8-backend.log`, and `artifacts/browser-smoke/issue8-frontend.log`.
- 2026-04-05: Created issue `#10` and branch `fix/10-fix-kamis-trend-overlay-fallback-and-labeling` to harden the already-landed KAMIS trend overlay after review found that per-item trend failures could blank the whole panel and the UI did not clearly distinguish card snapshots from retail-average history.
- 2026-04-05: Hardened `/api/market/produce` so trend-enrichment failures now populate `trend.unavailable_series` while preserving the live cards, updated the panel copy/tooltips to expose average-vs-snapshot semantics and 3y/5y/10y sample coverage, and refreshed smoke/unit tests to encode that contract explicitly.
- 2026-04-05: Revalidated issue `#10` locally with `npm --prefix frontend run lint`, `poetry run ruff check .`, `poetry run pytest`, `npm --prefix frontend run build`, and a direct live KAMIS fetch smoke that returned four featured series, zero unavailable overlays, and the expected `2026-03-21` to `2026-04-17` trend window.
- 2026-04-05: Created issue `#12` and branch `fix/12-rebalance-dashboard-panel-sizing-and-layout` to rebalance the dashboard shell after the weather, produce-price, RTR, and AI panels no longer fit their graph/card content comfortably in the current top-level layout.
- 2026-04-05: Widened the dashboard shell, split the sensor/AI row from the weather-produce-RTR row, and resized the produce-price chart/card composition plus the weather/RTR forecast grids so panel footprints better match the underlying content on desktop while preserving mobile stacking.
- 2026-04-05: Revalidated issue `#12` locally with `npm --prefix frontend run lint`, `poetry run ruff check .`, `poetry run pytest`, `npm --prefix frontend run build`, and desktop/mobile browser smokes on `127.0.0.1:8000/4173`; artifacts were saved under `artifacts/browser-smoke/issue12-layout-desktop.png`, `artifacts/browser-smoke/issue12-layout-mobile.png`, and `artifacts/browser-smoke/issue12-console.log`.
- 2026-04-05: Created issue `#16` and branch `data/16-show-wholesale-produce-prices-alongside-retail-market-panel` to expand the KAMIS market panel with wholesale snapshots instead of overloading the blocked RTR follow-up issue `#3`.
- 2026-04-05: Extended `/api/market/produce` so it now publishes `markets.retail` plus `markets.wholesale`, kept the 14-day / 3y-5y-10y trend overlay retail-only, and updated the frontend panel with a retail-vs-wholesale toggle plus explicit copy for the retail-only chart basis.
- 2026-04-05: Added a frontend compatibility normalization path so the produce-price panel no longer crashes if a running backend still serves the older retail-only payload during a staggered restart or deploy.
- 2026-04-05: Revalidated issue `#16` locally with `npm --prefix frontend run lint`, `poetry run ruff check .`, `poetry run pytest`, `npm --prefix frontend run build`, a direct live KAMIS fetch smoke that returned four retail and four wholesale featured items, and browser smokes on `127.0.0.1:8000/4173` plus a Playwright-mocked wholesale payload; artifacts were saved under `artifacts/browser-smoke/issue16-wholesale-market-panel.png` and `artifacts/browser-smoke/issue16-wholesale-panel-mocked.png`.
- 2026-04-05: Created issue `#18` and branch `feat/18-expand-smartgrow-ai-advisor-pipeline-for-tomato-and-cucumber-knowledge-assets` for the SmartGrow expansion loop, then rebased the blueprint and harness state from the old delivery follow-up onto a bounded phase-1 knowledge/data pipeline slice over the local tomato/cucumber corpus.
- 2026-04-05: Landed the phase-1 SmartGrow slice by adding a cached knowledge catalog over the local tomato/cucumber corpus, exposing `/api/knowledge/status` and `/api/knowledge/reindex`, auto-injecting crop-scoped knowledge context into `/api/ai/consult` and `/api/ai/chat`, and revalidating the touched tests plus frontend lint/build.
- 2026-04-05: Landed the phase-2a SmartGrow slice by adding compact pesticide/nutrient workbook normalization previews to the knowledge catalog, threading those previews into the existing AI knowledge context, and lazily exporting the backend `app` so service-level validation no longer imports the full simulator runtime unnecessarily.
- 2026-04-05: Landed the phase-2b SmartGrow slice by exporting workbook row references, adding a separate deterministic advisory service for pesticide/nutrient lookups, wiring new recommendation routes in `main.py`, and validating the service-level regressions while keeping `/api/knowledge/*` and `/api/ai/*` contracts unchanged.
- 2026-04-05: Landed the phase-2c advisory-discoverability slice by surfacing pesticide/nutrient recommendation route metadata back through the knowledge catalog and crop knowledge context, then validating the stable service-level contract with targeted unit tests.
- 2026-04-05: Landed the phase-2d advisory route-hardening slice by extracting `services/advisory_api.py`, fixing the invalid-crop 400→500 regression for pesticide/nutrient recommendation flows, moving route-contract tests off the heavy backend import path, and validating the stabilized seam with `tests/test_advisory.py`.
- 2026-04-05: Landed the phase-2e nutrient correction slice by adding `/api/nutrients/correction`, comparing submitted source/drain measurements against workbook baselines and guardrails through the lightweight advisory API seam, and extending knowledge-catalog discoverability plus unit coverage without changing `/api/knowledge/*` or `/api/ai/*` envelopes.
- 2026-04-06: Landed the phase-2f stock-tank-preparation slice by extending `/api/nutrients/correction` with nutrient balance rows and candidate fertilizer ranking, then revalidating the advisory/workbook/OpenAI regression suite while keeping the final fertilizer solver explicitly out of scope.
- 2026-04-06: Landed the phase-2g bounded sizing-draft slice by extending `/api/nutrients/correction` with macro-only single-fertilizer stoichiometric draft masses/concentrations, blocking incompletely modeled formulas, micronutrient unit ambiguities, and sodium-bearing or percentage-style products, and revalidating the advisory/workbook/OpenAI regression suite without claiming a final multi-fertilizer solver.
- 2026-04-06: Landed the phase-2h AI/UI integration slice by adding a `useSmartGrowKnowledge` frontend hook over `/api/knowledge/status`, surfacing advisory readiness and parser gaps in the existing AI advisor card, and exposing crop-scoped SmartGrow prompt shortcuts in the chat drawer without changing `/api/knowledge/*` or `/api/ai/*` envelopes.
- 2026-04-06: Landed the phase-2i frontend-advisor-contract slice by expanding the knowledge-status hook into a richer typed adapter and adding a dedicated SmartGrow surface panel that renders route metadata, request contracts, coverage, and limits from `/api/knowledge/status`, then revalidating the frontend lint/build surface without changing backend routes.
- 2026-04-06: Landed the phase-2j route-execution workspace slice by adding a typed SmartGrow route hook plus a bounded `AdvisorTabs` frontend workspace that executes the already-landed pesticide, nutrient, and correction routes, then revalidating frontend lint/build without introducing `/api/advisor/*` or new backend seams.
- 2026-04-06: Landed the phase-3a advisor-orchestration slice by adding `services/advisor_orchestration.py`, exposing `/api/advisor/summary`, `/api/advisor/tab/{tab_name}`, and `/api/advisor/chat`, rewiring the frontend AI summary/chat/execution hooks onto that additive contract, and revalidating with targeted ruff/pytest plus frontend lint/build.
- 2026-04-06: Landed the phase-4a prompt-level tab-taxonomy slice by expanding `AdvisorTabs` to the prompt-aligned `environment`/`physiology`/`work`/`pesticide`/`nutrient`/`harvest_market` registry, surfacing honest pending-state panels for unlanded domains through `/api/advisor/tab/{tab_name}`, moving nutrient correction under the nutrient tab as an advanced tool, localizing the new pending panels, and revalidating with frontend lint/build.
- 2026-04-06: Landed the phase-4b dashboard-fed work-tab slice by passing the existing summary/chat `dashboard` composite into planned-tab execution, normalizing `data` versus `currentData` inside advisor orchestration, and turning the `work` tab into a deterministic cultivation-priority/work-window surface, then revalidating with targeted ruff/pytest plus frontend lint/build while leaving the dedicated prompt-level `/api/work/recommend` route as a later gap.
- 2026-04-06: Landed the phase-4c dashboard-fed environment-tab slice by reusing the same summary/chat `dashboard` composite for `/api/advisor/tab/environment`, turning live telemetry, Daegu weather, RTR balance, and recent trends into deterministic current-state diagnosis plus immediate/24h/3-day steering cards, then revalidating with targeted ruff/pytest plus frontend lint/build while leaving the dedicated prompt-level `/api/environment/recommend` route as a later gap.
- 2026-04-06: Hardened the phase-4c environment-tab slice by keeping partial inside telemetry in monitoring-first mode, replacing the landed environment/work idle-state fallback away from the pending panel UX, aligning environment `missing_data` with actual dependencies instead of `metrics`, and revalidating with targeted ruff/pytest plus frontend lint/build.
- 2026-04-06: Landed the phase-4d dashboard-fed physiology-tab slice by reusing the same summary/chat `dashboard` composite for `/api/advisor/tab/physiology`, interpreting crop balance from canopy temperature, VPD, transpiration, stomatal conductance, photosynthesis, and crop-load signals, keeping the tab in monitoring-first mode when those core signals are incomplete, and revalidating with targeted ruff/pytest plus frontend lint/build.
- 2026-04-06: Landed the phase-4e dashboard-fed harvest-market-tab slice by threading a compact crop-matched produce-price snapshot through the shared advisor dashboard context, turning `/api/advisor/tab/harvest-market` into a deterministic shipment/quality tradeoff surface over forecast yield, weather risk, RTR balance, and market direction, and revalidating with targeted ruff/pytest plus frontend lint/build while leaving dedicated prompt-level work/environment routes as later gaps.
- 2026-04-06: Landed the phase-4f dedicated-route split by adding prompt-level `POST /api/environment/recommend` and `POST /api/work/recommend` backend entrypoints as thin delegates over the already-landed dashboard-fed advisor tabs, rewriting `orchestration.entrypoint` to the dedicated route while preserving the shared-tab delegate path, exposing those routes through `/api/knowledge/status`, and revalidating with targeted ruff/pytest plus frontend lint/build.
- 2026-04-06: Landed the phase-5a persistent knowledge-database slice by adding a SQLite-backed SmartGrow corpus store with document/chunk/entity rows, structured pesticide/nutrient workbook tables, crop-profile metadata, prompt-level scaffold tables, and PDF paragraph chunks, wiring `/api/knowledge/reindex` plus `/api/knowledge/status` onto that persisted database surface, and revalidating with targeted ruff/pytest, knowledge-status smoke, and frontend lint/build.
- 2026-04-06: Landed the phase-5b explicit knowledge-query slice by adding `POST /api/knowledge/query` over the persisted SQLite SmartGrow chunk store, publishing that retrieval seam through `/api/knowledge/status` plus crop knowledge context, and revalidating with targeted ruff/pytest, knowledge-status smoke, and frontend lint/build while keeping retrieval out of `/api/ai/*` and the current frontend execution seam.
- 2026-04-06: Landed the phase-5c RAG drawer consumer slice by adding `useRagAssistant`, a lazy `RagAssistantDrawer`, and explicit knowledge-search launch affordances in the existing AI advisor/chat surfaces, then revalidating with frontend lint/build while keeping `/api/knowledge/query` user-triggered and `/api/ai/*` unchanged.
- 2026-04-06: Hardened the phase-5c RAG drawer slice by separating database-missing UX from empty-result UX, aborting stale in-flight knowledge queries, disabling preset/filter controls during active search, and aligning the lazy fallback drawer position with the stacked chat layout before rerunning frontend lint/build.
- 2026-04-06: Landed the phase-5d retrieval-quality slice by adding a heuristic query router plus DB-backed hybrid reranking over `/api/knowledge/query`, merging FTS and lexical candidates, defaulting workbook/topic filters from detected intent, publishing the richer routed retrieval contract through the knowledge surface, and revalidating with targeted ruff/pytest, knowledge-status smoke, and frontend lint/build while keeping dense vector retrieval and automatic `/api/ai/*` coupling out of scope.
- 2026-04-06: Landed the phase-5e bounded advisor-consumer slice by adding `advisor_context_builder.py`, letting `/api/advisor/chat` build a compact chat-first retrieval context over the routed `/api/knowledge/query` seam, injecting only bounded evidence cards into the LLM dashboard payload, recording document/chunk/routing provenance internally, and revalidating with targeted ruff plus advisor/query/database/OpenAI pytest coverage while leaving `/api/advisor/summary` and `/api/ai/*` unchanged.
- 2026-04-06: Landed the phase-5f bounded advisor-summary slice by extending `advisor_context_builder.py` and `advisor_orchestration.py` so `/api/advisor/summary` now builds a compact summary-seeded retrieval context over the routed `/api/knowledge/query` seam, keeps raw provenance internal, lazily imports heavy simulator services in `main.py` to reduce Windows cold-start overhead, and revalidates with targeted ruff/pytest, full `tests/test_smoke.py`, knowledge-query/database/advisory/OpenAI pytest runs, and frontend lint/build.
- 2026-04-06: Landed the phase-5g frontend observability hardening slice by rendering the full multi-section AI advisor markdown instead of only `Executive Summary`, turning parsed advisor recommendations back into control-hint callback candidates, adding sensor-card loading/live/stale/offline states with relative update age plus sparklines, and revalidating with frontend lint/build while keeping the current backend contracts unchanged.
- 2026-04-07: Landed the phase-5h work-tab retrieval-sidecar slice by reusing the bounded tab-seeded advisor context builder inside the deterministic `work` tab, attaching additive retrieval summary plus evidence-card sidecars and internal knowledge-query provenance without changing the primary work-analysis contract, and revalidating with targeted ruff/pytest plus frontend lint/build.
- 2026-04-07: Landed the phase-5i environment-tab retrieval-sidecar slice by reusing the bounded tab-seeded advisor context builder inside the deterministic `environment` tab, attaching additive retrieval summary plus environment evidence-card sidecars and internal knowledge-query provenance without changing the primary environment-analysis confidence or missing-data contract, and revalidating with targeted ruff/pytest plus frontend lint/build.
- 2026-04-07: Landed the phase-5j retrieval-sidecar completion pass by reusing the bounded tab-seeded advisor context builder inside the deterministic `physiology` plus `harvest_market` tabs, rendering the remaining harvest/market frontend evidence parity alongside the already-landed physiology evidence surface, and revalidating with targeted ruff/pytest plus frontend lint/build.
- 2026-04-07: Landed the phase-6a common-action-contract pass by adding a shared `advisor_actions` horizon envelope to the landed `environment` plus `work` tabs, reusing a common timeline renderer in the frontend, and locking the cross-tab action schema with targeted golden assertions before fuller rule-engine and calculator phases.
- 2026-04-07: Landed the phase-6b work/environment rule-engine hardening pass by promoting daytime low-CO2 into an explicit environment steering action, surfacing richer deterministic rule-state fields across the landed environment and work tabs, and forcing the work tab into monitoring-first mode when planning or climate telemetry is incomplete instead of treating missing signals as zeros.
- 2026-04-07: Landed the phase-6b work/environment rule-engine hardening pass by adding richer deterministic rule-state fields (`operating_mode`, recovery/constraint, and `risk_flags`) to the landed environment/work tabs, layering additional workflow-aware work actions ahead of the generic decision helper, surfacing those state fields directly in the frontend, and revalidating with targeted ruff/pytest plus frontend lint/build.
- 2026-04-07: Landed the phase-6c pesticide registration/rotation hardening pass by rejecting cross-crop pesticide target leakage, canonicalizing workbook MOA labels, enriching rotation rows from the product master, returning registered-first unique-MOA programs with explicit manual-review flags and rotation-level mixing cautions, surfacing that safety burden through the knowledge catalog, and revalidating with targeted ruff/pytest plus frontend lint/build.
- 2026-04-07: Landed the phase-6d nutrient draft safety-gate hardening pass by rejecting negative source/drain mmol/L inputs, downgrading partial-input stock-tank drafts to provisional or blocked states, rechecking blocked analytes plus guardrails after macro-bundle assembly, surfacing bundle-execution review plus calculator defaults in the correction UI and knowledge catalog, and revalidating with targeted ruff/pytest plus frontend lint/build.
- 2026-04-07: Landed the phase-6e bounded drain-feedback target-planner pass by turning submitted drain deviations into clamped next-step nutrient targets for supported analytes, threading those effective targets into stock-tank preparation without relaxing the earlier safety gates, surfacing the planner state through the correction UI and knowledge catalog, and revalidating with targeted ruff/pytest plus frontend lint/build.
- 2026-04-07: Landed the phase-6f residual-safe bundle-alternative pass by computing an additive macro-bundle alternative that avoids modeled target overshoot when the selected bundle runs above target, surfacing that residual policy through the correction UI and knowledge catalog, and revalidating with targeted ruff/pytest plus frontend lint/build without silently replacing the selected bundle.
- 2026-04-07: Created issue `#19` and branch `feat/19-implement-model-first-smartgrow-advisor-with-crop-physiology-gas-exchange-and-sensitivity-engines` because the new SmartGrow directive is a new requirement that supersedes the old issue `#18` mainline.
- 2026-04-07: Realigned the blueprint, system brief, gap register, ADR, module spec, and `.rah` truth state around the model-first SmartGrow lane, preserving issue `#18` as the compatibility baseline and intentionally re-blocking implementation until the phase-1 persistence adapter and migration seam are frozen.
- 2026-04-07: Landed the bounded phase-1 model-runtime seam by adding adapter-backed `crop_models/` wrappers, a dedicated SQLite `model_state_store`, adapter metadata-safe snapshot dumping, and `POST /api/models/snapshot` plus `POST /api/models/replay`, then revalidated with `poetry run ruff check .`, `poetry run pytest -q`, `npm --prefix frontend run lint`, and `npm --prefix frontend run build`.
- 2026-04-07: Landed the reusable gas-exchange service layer by adding `gas_exchange_fvcb.py`, `stomatal_ball_berry.py`, and `canopy_integration.py`, wiring cucumber/tomato snapshot wrappers onto those helpers to publish upper/middle/bottom leaf activity and limiting-factor summaries, and revalidating the focused physiology/runtime tests before moving the next earliest gate to backend-only scenario/sensitivity work.
- 2026-04-07: Landed the first backend-only scenario/sensitivity runtime slice by adding `constraint_engine.py`, `scenario_runner.py`, `sensitivity_engine.py`, extending the SQLite sidecar with `scenario_runs`/`scenario_outputs`/`sensitivity_outputs`, exposing additive `POST /api/models/scenario` plus `POST /api/models/sensitivity`, and locking sign/trust-region/scenario-alignment behavior with targeted runtime tests before moving the next earliest gate to advisor-contract realignment.
- 2026-04-07: Landed the additive advisor-contract realignment slice by attaching a model-first `machine_payload.model_runtime` block to `/api/advisor/*`, `/api/environment/recommend`, and `/api/work/recommend` while preserving the issue #18 field names, shaping degraded summary/chat fallbacks consistently, and locking the new backend-only contract with orchestration tests before moving the next earliest gate to frontend counterfactual surfaces.
- 2026-04-07: Landed the first frontend counterfactual/model-aware surface slice by extending the typed advisor hook with additive `model_runtime`, adding a shared `AdvisorModelRuntimePanel`, wiring it into the `environment`, `physiology`, and `work` tabs with old-payload guards, hardening missing `violated_constraints` handling after review, and revalidating with `poetry run ruff check .`, `poetry run pytest`, `npm --prefix frontend run lint`, `npm --prefix frontend run build`, plus a focused reviewer pass with no blockers before moving the next earliest gate to `harvest_market` and Assistant runtime alignment.
- 2026-04-07: Landed the bounded `harvest_market` plus Assistant runtime-alignment slice by wiring the shared `AdvisorModelRuntimePanel` into the `harvest_market` tab, threading `machine_payload.model_runtime` through each AI chat reply as a compact snapshot-aware runtime strip, fixing the reviewer-found `unavailable` vs `monitoring-first` status-label mismatch, and revalidating with `poetry run ruff check .`, `poetry run pytest`, `npm --prefix frontend run lint`, `npm --prefix frontend run build`, plus a reviewer re-check with no remaining blockers before moving the next earliest gate to the dashboard summary runtime surface.
- 2026-04-07: Landed the bounded summary-runtime plus weather-resilience slice by threading `/api/advisor/summary` `machine_payload.model_runtime` into a compact `AiAdvisor` runtime strip with stale-request guards, adding cached/synthetic fallback handling to the Daegu weather service and explicit cached/fallback provider labeling in the weather panel, and revalidating with `poetry run ruff check .`, `poetry run pytest`, `npm --prefix frontend run lint`, and `npm --prefix frontend run build`.
- 2026-04-07: Hardened the issue `#19` knowledge/advisor validation lane by replacing local-workbook-only advisory assertions with synthetic-fixture-backed CI-safe checks, sanitizing knowledge-catalog asset paths even when `data_root` is outside `REPO_ROOT`, refreshing summary analysis when produce-price data arrives after telemetry, and revalidating both local ladders plus green push/pull_request GitHub Actions runs for PR `#20`.
- 2026-04-07: Merged PR `#20` into `main` at `ecbd9b7`, closed issue `#19` with project status `Done`, and synced the local `main` branch to the merged baseline after the full validation ladder and green GitHub Actions.
- 2026-04-07: Closed superseded issue `#18`, opened issue `#21` with branch `feat/21-implement-work-event-driven-compare-loop-for-smartgrow-advisor-tradeoffs`, and reset the harness runtime state around the new work-event-driven compare loop before further implementation.
- 2026-04-07: Landed the first issue `#21` work-event compare slice by adding `ModelStateStore` read APIs, replay-diff `work_event_compare` payloads over persisted cucumber/tomato snapshots and candidate `leaf_removal` / `fruit_thinning` actions, rendering a separate compare block in `WorkTab`, and revalidating with `poetry run ruff check .`, `poetry run pytest`, `npm --prefix frontend run lint`, and `npm --prefix frontend run build`.
- 2026-04-07: Hardened the issue `#21` work-event compare slice after review by threading `greenhouse_id` through `/api/advisor/tab/work`, preventing malformed persisted event payloads or unavailable model-state stores from crashing the `work` tab, adding a request-contract smoke test for greenhouse forwarding, and revalidating the focused pytest/ruff checks before rerunning the full validation ladder.
- 2026-04-07: Landed exact additive advisor surface routes for the model-first SmartGrow lane by adding `/api/advisor/environment`, `/api/advisor/physiology`, `/api/advisor/work-tradeoff`, and `/api/advisor/harvest` over the existing tab surfaces, flattening `work_event_compare` into a directive-aligned top-level tradeoff contract, aligning `/api/knowledge/status` discovery with the new exact routes, and revalidating with targeted ruff/pytest plus the full backend/frontend validation ladder.
- 2026-04-07: Landed the next issue `#21` work-event compare hardening slice by replacing the bounded candidate generator with crop-specific cucumber/tomato agronomy and ranking logic, adding leaf-guard and sink-overload matrix coverage, extending the frontend typed contract and `WorkTab` detail surface for agronomy flags plus 14-day fruit-DM/LAI deltas, and revalidating with `poetry run ruff check .`, `poetry run pytest`, `npm --prefix frontend run lint`, and `npm --prefix frontend run build`.
- 2026-04-07: Closed the last local issue `#21` hardening gap by adding a minimal frontend Vitest/RTL harness for `WorkTab` `work_event_compare`, syncing `frontend/package.json` and `vite.config.ts` for jsdom test execution, and revalidating with `poetry run ruff check .`, `poetry run pytest`, `npm --prefix frontend run test`, `npm --prefix frontend run lint`, and `npm --prefix frontend run build` while keeping PR `#22` as the active delivery lane.
- 2026-04-07: Merged PR `#22` into `main` at `441800e`, confirmed issue `#21` auto-closed to project status `Done`, and reset the harness runtime state back to a no-active-loop merged baseline so the next non-trivial change must start from a fresh issue/branch.
- 2026-04-07: Resumed issue `#23` on `feat/23-dashboard-layout-kpi-first-hierarchy`, extracted `AppShell`/shared loading and overlay fallback components, rewired AI summary/chat onto locale-aware structured display payloads, tightened Korean copy on visible advisor surfaces, and revalidated with `npm --prefix frontend run lint`, `npm --prefix frontend run test`, and `npm --prefix frontend run build`.
- 2026-04-07: Hardened issue `#23` sensor semantics by attaching per-field availability and last-received timestamps to streamed sensor points, nulling unavailable series in chart selectors, removing KPI health contradictions when primary sensors are missing/offline, restoring the mobile locale toggle, and revalidating the full ladder with `poetry run ruff check .`, `poetry run pytest`, `npm --prefix frontend run lint`, `npm --prefix frontend run test`, and `npm --prefix frontend run build`.
- 2026-04-08: Hardened issue `#23` Korean visible UI labels by localizing SmartGrow-facing surface titles, advisor-tab descriptive copy, work-event compare labels, and stage/medium/common status token display helpers, then revalidated with `poetry run ruff check .`, `poetry run pytest`, `npm --prefix frontend run lint`, `npm --prefix frontend run test`, and `npm --prefix frontend run build`.
- 2026-04-08: Captured the refreshed issue `#23` dashboard screenshot at `screenshots/issue23-after-dashboard-ko.png`; earlier comparison captures remain in `screenshots/before_fullpage.png` and `screenshots/after_fullpage.png`.
- 2026-04-08: Added a follow-up Korean-visible-UI hardening slice on issue `#23`, removing remaining user-facing `AI`/`RAG`/`drawer` wording on key surfaces, replacing raw knowledge-route exposure with descriptive labels, localizing additional WorkTab tokens, and re-validating with `poetry run ruff check .`, `poetry run pytest`, `npm --prefix frontend run lint`, `npm --prefix frontend run test`, and `npm --prefix frontend run build`.
- 2026-04-08: Pushed commit `7abf02a` (`fix: harden korean dashboard copy surfaces`) to PR `#24`, so GitHub Actions are rerunning against the latest issue `#23` localization hardening slice.
- 2026-04-08: Committed issue `#23` frontend stabilization as `5b1d1b1`, pushed `feat/23-dashboard-layout-kpi-first-hierarchy`, and confirmed PR `#24` is the active remote validation lane with checks pending.
- 2026-04-08: Added a farmer-facing terminology cleanup slice on issue `#23`, replacing academic/system-heavy labels with grower-friendly Korean wording, hiding raw evidence/provenance cards from user-facing advisor tabs, localizing additional backend advisory summaries, and re-validating with `poetry run ruff check .`, `poetry run pytest`, `npm --prefix frontend run lint`, `npm --prefix frontend run test`, and `npm --prefix frontend run build`.
- 2026-04-08: Hardened the issue `#23` KPI strip layout so warning-state `실시간/경고/최근 수집` text wraps safely inside each card and the top status row also wraps instead of colliding, then revalidated with `npm --prefix frontend run lint`, `npm --prefix frontend run test`, and `npm --prefix frontend run build`.
- 2026-04-08: Replaced the old non-interactive SmartGrow surface catalog with actionable quick-entry cards that open the actual advisor tabs, translated the remaining nutrient/correction limitation copy into grower-friendly Korean in both frontend and backend payloads, and revalidated with `npm --prefix frontend run lint`, `npm --prefix frontend run test`, `npm --prefix frontend run build`, `poetry run ruff check .`, and `poetry run pytest`.
- 2026-04-08: Upgraded issue `#23` knowledge search into a contextual grower-facing surface by seeding the drawer from dashboard/advisor/assistant intent, auto-running crop-matched preset queries, exposing routed scope summaries in Korean instead of raw route metadata, hiding the internal reindex recovery path, and revalidating with `npm --prefix frontend run test`, `npm --prefix frontend run lint`, `npm --prefix frontend run build`, `poetry run ruff check .`, and `poetry run pytest`.
- 2026-04-08: Pushed issue `#23` contextual knowledge-search slice as commit `f50834e`, confirmed PR `#24` head updated, and watched refreshed GitHub Actions until both Backend Validation and Frontend Validation returned green on the new head.
- 2026-04-08: Hardened the pesticide advisor on issue `#23` by preferring Hangul product names in Korean mode, surfacing clearer cycle/rotation/backup-option guidance, deriving grower-facing recommendation reasons from locale-safe backend codes instead of backend narrative text, preserving compatibility with legacy payloads that omit additive rotation fields, and revalidating with `npm --prefix frontend run test`, `npm --prefix frontend run lint`, `npm --prefix frontend run build`, `poetry run ruff check .`, and `poetry run pytest`.
- 2026-04-08: Closed the remaining pesticide-tab locale leak on issue `#23` by translating backend limitation strings on the frontend, extending the pesticide Vitest harness to lock Korean and English limitation rendering alongside the earlier product/cycle/rotation detail surface, and revalidating with `npm --prefix frontend run test`, `npm --prefix frontend run lint`, `npm --prefix frontend run build`, `poetry run ruff check .`, and `poetry run pytest`.
- 2026-04-08: Pushed commit `6c590a5` (`fix: localize pesticide advisor limitation copy`) to PR `#24`, then watched refreshed GitHub Actions Backend/Frontend Validation return green on the latest issue `#23` head before syncing post-validation harness truth.
- 2026-04-08: Merged PR `#24` into `main` at `25cabd2`, confirmed issue `#23` is closed, fast-forwarded local `main`, and synced the harness runtime state back to a no-active-loop merged baseline.
- 2026-04-08: Created issue `#25` and branch `fix/25-localize-remaining-grower-facing-advisory-warnings-and-hide-non-actionable-caveats` to remove the last grower-visible English/internal caveats, keep non-actionable implementation warnings out of the UI, and register unresolved scope only in internal gap tracking.
- 2026-04-08: Localized the remaining grower-facing pesticide warning strings, replaced raw AI fallback/error wording with generic Korean-safe copy, hid non-actionable nutrient/correction/provider/meta caveats from dashboard/advisor/chat surfaces, and updated regression coverage plus the gap register so unresolved implementation scope stays internal instead of showing up in the grower UI.
- 2026-04-08: Revalidated issue `#25` locally with `npm --prefix frontend run test`, `npm --prefix frontend run lint`, `npm --prefix frontend run build`, `poetry run ruff check .`, and `poetry run pytest`; the warning-cleanup slice is now green locally and ready for commit/push/PR.
- 2026-04-08: Committed issue `#25` as `07e2953` (`fix: localize grower-facing warning cleanup`), pushed branch `fix/25-localize-remaining-grower-facing-advisory-warnings-and-hide-non-actionable-caveats`, and opened PR `#26` for remote validation.
- 2026-04-08: Refreshed the issue `#25` local validation ladder after rechecking the warning-cleanup surfaces: `npm --prefix frontend run test`, `npm --prefix frontend run lint`, `npm --prefix frontend run build`, `poetry run ruff check .`, and `poetry run pytest` all stayed green with `126 passed, 26 warnings`.
- 2026-04-08: Merged PR `#26` into `main` at `b90e959`, confirmed issue `#25` is closed with project status `Done`, and reset the harness runtime state back to a no-active-loop merged baseline.
- 2026-04-08: Resumed work on issue `#27` branch `feat/27-overhaul-rtr-around-internal-model-optimizer-and-area-aware-projections` for the internal-model-only RTR optimizer overhaul, keeping the existing model-first SmartGrow runtime as the donor computation stack.
- 2026-04-08: Revalidated the landed RTR backend slice with focused pytest runs (`tests/test_rtr_optimizer.py` plus `tests/test_smoke.py -k rtr`) after stabilizing the fake-scipy optimizer harness, confirming the new `/api/rtr/*` service lane is locally green before frontend work resumes.
- 2026-04-08: Synced `.rah` state for issue `#27`, marking the backend optimizer lane as pass and the next earliest pending gate as the frontend optimizer surface with area-unit projection and `AREA_M2` hardcode removal.
- 2026-04-08: Landed the issue `#27` frontend RTR optimizer slice by wiring `AreaUnitContext` and `AreaUnitPanel` into the app shell, replacing the hardcoded RTR-facing `AREA_M2` path, adding `useRtrOptimizer` over `/api/rtr/state|optimize|scenario|sensitivity|area-settings`, rendering `RTROptimizerPanel` with baseline fallback, and extending frontend/backend contracts plus dedicated hook/component tests for solver metadata, area persistence, optimizer enable flags, and locale-safe explanation rendering.
- 2026-04-08: Revalidated issue `#27` with the full local ladder: `npm --prefix frontend run test`, `npm --prefix frontend run lint`, `npm --prefix frontend run build`, `poetry run ruff check .`, and full `pytest` now pass (`134 passed, 28 warnings`) after the optimizer + area-aware projection slice landed.
- 2026-04-08: Committed issue `#27` as `2a537bc` (`feat: land internal-model RTR optimizer surface`), pushed `feat/27-overhaul-rtr-around-internal-model-optimizer-and-area-aware-projections`, and opened PR `#28` (`[Model Change] Overhaul RTR around internal-model optimizer and area-aware projections`) with the repo helper script.
- 2026-04-08: Confirmed PR `#28` GitHub Actions Backend/Frontend validation both passed, merged the PR into `main` at `531f344`, confirmed issue `#27` closed with project status `Done`, and fast-forwarded local `main` to the merged RTR optimizer baseline.
- 2026-04-08: Reopened the blocked RTR calibration follow-up by moving issue `#3` to `Running`, creating the fresh issue-based branch `data/3-rtr-grower-window-intake-prep`, and treating the next bounded slice as grower-window intake prep rather than unsupported heuristic window replacement.
- 2026-04-08: Synced `.rah` for issue `#3` and added a dedicated grower-window intake restart plan so the next actual calibration step starts only after approved tomato/cucumber good-production periods are supplied.
- 2026-04-08: Corrected the post-merge RTR follow-up truth on `main` by reopening issue `#3`, keeping its project status at `Blocked`, and resetting `.rah` so the merged baseline carries the intake-prep note while showing no active implementation branch until approved windows are supplied.
- 2026-04-08: Hardened issue `#3` intake prep by tagging current RTR windows as `heuristic-demo`, adding machine-checked approval metadata validation to the good-window loader, exposing approved/demo counts in the calibration CLI, and revalidating with `poetry run pytest tests/test_rtr_profiles.py`, `poetry run pytest tests/test_smoke.py -k rtr`, `poetry run ruff check .`, and full `poetry run pytest`.
- 2026-04-08: Merged PR `#35` into `main` at `3098a21`, confirmed issue `#34` closed with project status `Done`, and fast-forwarded local `main` to the merged RTR decision-surface baseline after the remote Backend/Frontend validation checks passed.
- 2026-04-08: Synced `.rah` and implementation-gate notes after issue `#34` merge so the baseline again shows no active implementation branch while issue `#3` remains intentionally blocked on grower-approved RTR calibration windows.
- 2026-04-08: Created issue `#39` with branch `feat/39-complete-rtr-scenario-and-sensitivity-control-surfaces` to close the remaining RTR control-surface gaps after the merged issue `#37` baseline, specifically fixed offset compare rows and non-temperature sensitivity lanes.
- 2026-04-08: Landed the issue `#39` control-surface follow-up by adding fixed baseline offset scenario rows (`-0.3C`, `+0.3C`, `+0.6C`), screen-bias finite-difference sensitivity rows for humidity/disease risk, explicit frontend label/type support for those rows, and backend/frontend regression coverage, then revalidated with `npm --prefix frontend run lint`, `npm --prefix frontend run test`, `npm --prefix frontend run build`, `poetry run ruff check .`, `poetry run pytest`, and `git diff --check`.
- 2026-04-08: Committed issue `#39` as `4a2d8a8` (`feat: extend RTR compare and screen sensitivity surfaces`), pushed `feat/39-complete-rtr-scenario-and-sensitivity-control-surfaces`, opened PR `#40`, and moved the project item to `Validating` while GitHub Actions Backend/Frontend checks started on the new head.
- 2026-04-08: Confirmed PR `#40` GitHub Actions Backend/Frontend validation both passed, merged the PR into `main` at `def5de7`, closed issue `#39` with project status `Done`, and fast-forwarded local `main` to the merged RTR control-surface baseline.
- 2026-04-09: Created issue `#47` and branch `feat/47-close-remaining-phytosync-shadcn-redesign-prompt-gaps` from the merged issue `#45` UI baseline to close the remaining `codex_phytosync_shadcn_redesign_prompt.md` gaps without editing the dirty root checkout.
- 2026-04-09: Landed the issue `#47` frontend gap-closure slice by wiring route-driven `PhytoSync` workspace modules, shared design-token/style layers, a dedicated ask/search workspace, Korean visible-copy cleanup, and tab/navigation accessibility hardening, then revalidated with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, and `npm --prefix frontend run build`.
- 2026-04-09: Captured fresh Playwright viewport screenshots for the validating issue `#47` branch at `artifacts/screenshots/issue47-overview.png` and `artifacts/screenshots/issue47-ask.png` during preview-server smoke checks.
- 2026-04-09: Committed issue `#47` as `f07dad5` (`feat: close remaining phytosync redesign prompt gaps`), pushed `feat/47-close-remaining-phytosync-shadcn-redesign-prompt-gaps`, opened PR `#48`, and moved the project item to `Validating` while GitHub Actions Backend/Frontend checks started on the latest head.
# 2026-04-09 - issue61 coral stay routed shell kickoff

- Opened issue `#61` (`[Hypothesis] Rebuild PhytoSync into Coral Stay routed shell`) and checked out branch `hyp/61-rebuild-phytosync-into-coral-stay-routed-shell`.
- Audited the current frontend baseline and confirmed:
  - `frontend/src/main.tsx` already mounts `BrowserRouter`
  - `frontend/src/components/ui/*` already contains a shadcn-style primitive layer
  - `frontend/src/routes/*` exists, but most route/page composition still centralizes in `frontend/src/App.tsx`
  - current `AppShell` still uses an oversized `max-w-[2140px]` shell that does not enforce the requested `1280px` canvas rhythm
  - current theme is warm, but still mixes green/forest/violet semantics instead of a true coral-first primary palette
- Set the new implementation target for phase 1:
  - true top-level route map `/overview|/control|/rtr|/crop-work|/resources|/alerts|/assistant|/settings`
  - shared coral app shell and page header rhythm
  - dedicated page components instead of generic `SectionRouteFrame`/`MainDashboard` wrappers
  - preserve existing backend API, optimizer, advisor, and area-projection contracts while changing IA and presentation
- 2026-04-09: Landed the first issue `#61` stabilization slice by removing the dedicated `charts-vendor` manual chunk from `frontend/vite.config.ts` to eliminate the production `react-vendor` ↔ `charts-vendor` circular runtime crash, trimming `/overview` so it no longer embeds the `ControlPanel`, and adding `frontend/src/App.routing.test.tsx` to lock direct URL entry plus sidebar-driven route transitions.
- 2026-04-09: Revalidated the issue `#61` slice locally with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, and `npm --prefix frontend run build` (`19 test files, 53 tests passed`), then passed Playwright route smoke on `http://127.0.0.1:4180` across `/overview`, `/control`, `/rtr`, `/crop-work`, `/resources`, `/alerts`, `/assistant`, and `/settings`; the remaining console errors are backend-offline fetch failures to `127.0.0.1:8003`, not frontend route/runtime crashes.
- 2026-04-09: Captured the routed-shell screenshot for the current issue `#61` slice at `artifacts/screenshots/issue61-overview-route-shell.png`.
- 2026-04-09: Extracted `/overview`, `/control`, and `/rtr` page assembly out of `frontend/src/App.tsx` into dedicated route-level composition containers (`frontend/src/pages/overview-route-page.tsx`, `control-route-page.tsx`, `rtr-route-page.tsx`), kept the routed shell behavior intact, revalidated with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, and `npm --prefix frontend run build`, then reran Playwright live smoke on `http://127.0.0.1:4180` with backend `127.0.0.1:8003` and confirmed zero browser console errors on `/overview`, `/control`, and `/rtr`.
- 2026-04-09: Captured the live routed overview after page-composition extraction at `artifacts/screenshots/issue61-overview-route-extracted.png`.
- 2026-04-09: Extracted `/resources`, `/alerts`, `/assistant`, and `/settings` page assembly out of `frontend/src/App.tsx` into dedicated route-level composition containers (`frontend/src/pages/resources-route-page.tsx`, `alerts-route-page.tsx`, `assistant-route-page.tsx`, `settings-route-page.tsx`), revalidated with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, and `npm --prefix frontend run build`, then restarted the stale backend listener on `127.0.0.1:8003` and confirmed zero Playwright console errors on `/resources`, `/alerts`, `/assistant`, and `/settings`.
- 2026-04-09: Extended `frontend/src/App.routing.test.tsx` so direct `/resources` entry plus `/alerts` sidebar navigation are locked as dedicated pages, then captured the extracted route surfaces at `artifacts/screenshots/issue61-resources-route-extracted.png` and `artifacts/screenshots/issue61-assistant-route-extracted.png`.
- 2026-04-09: Fixed the `assistant` right-rail compression on issue `#61` by delaying the summary split in `frontend/src/pages/assistant-page.tsx`, adding a compact rail-specific launcher layout to `frontend/src/components/SmartGrowSurfacePanel.tsx`, revalidating with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, and `npm --prefix frontend run build`, then confirming zero Playwright console errors and saving the corrected screen at `artifacts/screenshots/assistant-clipping-fixed.png`.
- 2026-04-09: Extracted `/crop-work` assembly out of `frontend/src/App.tsx` into `frontend/src/pages/crop-work-route-page.tsx`, retired the legacy `/ask` `MainDashboard` frame by redirecting `/ask#...` into `/assistant#...`, added App routing regressions for both paths, revalidated with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, and `npm --prefix frontend run build`, then confirmed zero Playwright console errors on `/crop-work` and `/ask#ask-search` while saving `artifacts/screenshots/issue61-crop-work-route-extracted.png` and `artifacts/screenshots/issue61-ask-redirected-to-assistant.png`.
- 2026-04-09: Replaced the remaining `/growth`, `/nutrient`, `/protection`, and `/harvest` compatibility routes with a dedicated `frontend/src/pages/advisor-lane-route-page.tsx` container instead of `SectionRouteFrame`/`MainDashboard`, wired those routes through `frontend/src/App.tsx`, added routing regressions for nutrient and harvest lane entry in `frontend/src/App.routing.test.tsx`, revalidated with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, and `npm --prefix frontend run build`, then confirmed zero Playwright console errors on `/growth#growth-work` and `/nutrient#nutrient-tool` while saving `artifacts/screenshots/issue61-growth-lane-route-page.png` and `artifacts/screenshots/issue61-nutrient-lane-route-page.png`.
- 2026-04-09: Hardened the routed advisor-lane intent on issue `#61` by making the overview hero advisor CTA open `/growth` through `handleOpenAdvisorTabs('environment')` instead of a plain `/crop-work` navigation, exposing `AdvisorTabs`/surface CTA props in `frontend/src/App.routing.test.tsx` mocks, and locking `initialTab` plus nutrient-correction preservation across `/growth`, `/nutrient`, `/protection`, `/harvest`, and `/assistant` flows; local validation stayed green with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, and `npm --prefix frontend run build` (`19 files, 61 passed`).
- 2026-04-09: Trimmed another dead compatibility slice from `frontend/src/App.tsx` by removing the legacy `/growth`-only branches from `lowerFoldSurface` and `bottomRowSurface`, which were no longer reachable after `/crop-work` and `/growth` moved to dedicated route pages; revalidated with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, and `npm --prefix frontend run build` (`19 files, 61 passed`).
- 2026-04-10: Resynced the issue `#61` control plane by updating `Phytoritas.md`, `docs/architecture/`, `.rah/state/status.json`, `.rah/state/gates.json`, `.rah/state/memento_status.json`, `.rah/memory/*`, and the issue `#61` wakeup/current-loop packet so the recursive-architecture helper surfaces stop resuming from the stale RTR-era branch/case.
- 2026-04-10: Rehydrated Memento for the issue `#61` case, mirrored `context()` and `recall()` results into `.rah/memory/` ledgers, recorded `tool_feedback()` for recall search event `1373`, and reran `rah.py doctor|status|resume` until identity/freshness checks passed.
- 2026-04-10: Revalidated the current issue `#61` worktree after the control-plane sync with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, `npm --prefix frontend run build`, `poetry run ruff check .`, and full `poetry run pytest` (`149 passed, 34 warnings`).
- 2026-04-10: Extracted the last legacy-only route-frame composition out of `frontend/src/App.tsx` into `frontend/src/routes/LegacyCompatibilityFrame.tsx`, rewired `/overview|/control|/resources|/alerts/legacy` to the new quarantine module, and broadened `frontend/src/App.routing.test.tsx` so all four compatibility paths stay pinned to the mocked `MainDashboard` frame.
- 2026-04-10: Fixed the follow-up lint regression by removing the dead `ControlPanel` import from `frontend/src/App.tsx` and adding `setSectionTabSelections` to the `handleSectionTabSelect` `useCallback` dependency list, then revalidated with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, `npm --prefix frontend run build`, `poetry run ruff check .`, and `poetry run pytest` (`149 passed, 34 warnings`; frontend `19 files, 65 passed`).
- 2026-04-10: Retired the remaining `/overview|/control|/resources|/alerts/legacy` runtime seam by replacing it with explicit canonical redirects in `frontend/src/App.tsx`, deleting `frontend/src/routes/LegacyCompatibilityFrame.tsx`, and removing the dead `SectionRouteFrame` / `MainDashboard` / old route-wrapper files from `frontend/src/`; `frontend/src/App.routing.test.tsx` now locks all four legacy URLs to the correct routed pages instead of a mocked compatibility frame, and the validation ladder stayed green with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, `npm --prefix frontend run build`, `poetry run ruff check .`, `poetry run pytest`, and `rah.py doctor|status|resume`.
- 2026-04-10: Canonicalized the knowledge section to `assistant` / `/assistant` in `frontend/src/routes/phytosyncSections.ts`, kept `/ask` redirect plus `ask-*` panel ids as bounded compatibility, reran `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, `npm --prefix frontend run build`, `poetry run ruff check .`, `poetry run pytest` (`149 passed, 34 warnings`), and `rah.py doctor|status|resume`, then recorded Memento recall feedback for search events `1407` and `1415`.
- 2026-04-10: Merged PR `#62` into `main`, closing issue `#61` and moving the project item to `Done`; local `main` was fast-forwarded to merge commit `895e52c`.
- 2026-04-10: Opened issue `#63` (`[Doc] Sync issue61 merged harness state on main`) on branch `docs/63-sync-issue61-merged-harness-state-on-main` to realign `.rah` after the merge, rewrote `status/current_loop/wakeup` for the post-issue61 `main` baseline, and removed tracked session-only Memento snapshot JSON files from the git index so future regenerations stay untracked.

- 2026-04-10: Created issue `#74` on branch `fix/74-polish-remaining-control-panel-and-section-copy-residue`, kept the scope bounded to `ControlPanel` fallback labels plus `control-page` / `control-route-page` / `phytosyncSections.ts` copy residue, and revalidated locally with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, `npm --prefix frontend run build`, `poetry run ruff check .`, `poetry run pytest`, and `git diff --check`.
- 2026-04-10: Committed issue `#74` as `9b8836b` (`fix: polish remaining control copy residue`), pushed `fix/74-polish-remaining-control-panel-and-section-copy-residue`, opened PR `#75`, and moved the project item to `Validating`.
- 2026-04-10: Confirmed PR `#75` GitHub Actions Backend/Frontend validation both passed, merged the PR into `main` at `6c47640`, closed issue `#74` with project status `Done`, and fast-forwarded local `main` to the merged compact-control-copy baseline.
- 2026-04-10: Opened issue `#76` (`[Doc] Sync issue74 merged harness state on main`) on branch `docs/76-sync-issue74-merged-harness-state-on-main` to realign `.rah` around the post-issue74 merged baseline on `main`.
- 2026-04-10: Opened issue `#78` (`[Doc] Refresh blueprint pointers and rehydrate harness runtime seeds`) on branch `docs/78-refresh-blueprint-pointers-and-rehydrate-harness-runtime-seeds`, refreshed the stale root/mirror `Phytoritas.md` pointers away from the retired issue65 lane, recreated the ignored local `.rah` runtime seed files from the harness bootstrap helpers, and reran `automation/rah.py doctor|status|resume` until doctor returned warn-only instead of fail.
- 2026-04-10: Opened issue `#80` on branch `fix/80-realign-overlapping-layout-tiles-and-panels`, removed the fragile overview/crop-work `xl:auto-rows-[88px]` + `row-span-*` coupling, added a compact two-chart overview variant in `Charts.tsx`, captured before/after screenshots for `/overview` and `/crop-work`, and revalidated with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, `npm --prefix frontend run build`, `poetry run ruff check .`, `poetry run pytest`, and `git diff --check`.
- 2026-04-10: Opened issue `#82` on branch `fix/82-fix-remaining-compact-shell-clipping-and-overflow`, added compact narrow-rail modes to `TodayBoard` and `AlertRail`, applied them to `/overview`, `/control`, and `/crop-work` right-side rails, captured fresh Playwright screenshots for all three pages, and revalidated with `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, `npm --prefix frontend run build`, `poetry run ruff check .`, `poetry run pytest`, and `git diff --check`.
- 2026-04-11: Opened issue `#94` on branch `fix/94-finalize-post-issue92-dashboard-polish-and-precision-runtime-recommendations`, split the validated dirty worktree into frontend and backend commits, excluded the local `overview-current.png` screenshot from git status via `.git/info/exclude`, pushed the branch, and opened PR `#95`.
- 2026-04-11: The issue `#94` branch stayed green on the full local ladder: `npm --prefix frontend run lint`, `npm --prefix frontend run test -- --pool=threads`, `npm --prefix frontend run build`, `poetry run ruff check .`, and `poetry run pytest`.
- 2026-04-11: Merged PR `#95` into `main` at `ff0a92a`, closed issue `#94`, moved the project item to `Done`, and returned the local repo to a clean `main` baseline.
- 2026-04-11: Opened docs issue `#96` on branch `docs/96-sync-issue94-merged-harness-state-on-main` to realign tracked `.rah` state with the merged main baseline without reopening product scope.
- 2026-04-11: Merged PR `#97` into `main` at `daaef46`, closed docs issue `#96`, and confirmed the repo stayed clean on `main`.
- 2026-04-11: Opened docs issue `#98` on branch `docs/98-sync-post-issue96-harness-state-on-main` to make tracked `.rah` state stable on clean `main` by pointing it at the blocked issue `#3` backlog instead of the retired docs sync lane.
