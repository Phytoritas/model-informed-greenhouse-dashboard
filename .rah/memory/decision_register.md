# Decision Register

- 2026-04-02: Treat the source project `dashboard-eng_1.1` as an intake source, not as a directory layout to mirror blindly.
- 2026-04-02: Migrate the backend runtime before the frontend so the package and config boundaries can stabilize under the current validation ladder.
- 2026-04-02: Use the source `frontend/` Vite app as the canonical frontend migration path for this repository.
- 2026-04-02: Keep the source `Tomato_Env.CSV` and `Cucumber_Env.CSV` as local sample fixtures in `data/` so the migrated frontend can auto-start simulations without extra setup.
- 2026-04-02: Store operational settings and crop-config mutations per crop in backend state instead of mutating a single shared dashboard config.
- 2026-04-02: Treat `localhost` and `127.0.0.1` as equivalent local browser hosts in frontend defaults and backend CORS so the dev dashboard smoke does not depend on one exact hostname.
- 2026-04-02: Keep the dashboard shell eager, but move chart-heavy and report/chat-heavy UI modules behind lazy boundaries with explicit vendor chunk grouping so production chunks stay below the Vite warning threshold without breaking the built preview smoke.
- 2026-04-03: Switch the backend AI helper from Gemini to the OpenAI Responses API and load `OPENAI_API_KEY` from the repo-root `.env` so live AI responses are enabled through the backend environment.
- 2026-04-07: Treat issue `#18` as the preserved knowledge/RAG/advisory baseline and open issue `#19` for the new model-first SmartGrow requirement because repo policy requires a new issue for a new requirement.
- 2026-04-07: Make model state and bounded scenario/sensitivity outputs higher priority than retrieval context; retrieval remains explanation and provenance support only.
- 2026-04-07: Reuse the legacy tomato/cucumber model code as donor logic for the first `crop_models/` service layer instead of inventing a fresh physiology stack before the bounded phase-1 interfaces exist.
- 2026-04-07: Freeze the phase-1 persistence adapter as a dedicated SQLite sidecar at `artifacts/models/model_runtime.sqlite3` instead of reusing the knowledge database or ad-hoc JSON files.
- 2026-04-07: Use the existing adapter `dump_state()` / `load_state()` seam as the authoritative phase-1 migration boundary, then store a versioned normalized snapshot beside that raw adapter state for replay and later advisor/runtime promotion.
- 2026-04-07: Extract reusable FvCB, Ball-Berry, and canopy-integration helpers before any scenario/sensitivity work, but keep growth, dry-matter partitioning, and energy-balance orchestration inside the legacy crop models for this slice.
- 2026-04-07: Let the normalized cucumber/tomato snapshot wrappers consume the new physiology helpers first so upper/middle/bottom leaf activity and limiting-factor summaries become available without mutating the active simulator/runtime seam.
- 2026-04-07: Add scenario/sensitivity/constraint runtime as a new `/api/models/*` family first, and keep `/api/advisor/*` plus existing frontend payload names untouched until the additive advisor-contract migration phase.
- 2026-04-07: Keep the preserved advisor/frontend field names as the compatibility shell and attach model-backed runtime outputs only under additive `machine_payload.model_runtime`, using a dashboard-synthesized runtime snapshot until the frontend counterfactual phase is ready to consume that block explicitly.
- 2026-04-07: Use one shared `AdvisorModelRuntimePanel` as the additive right-column consumer for `environment`, `physiology`, and `work`, and suppress it entirely when older payloads omit `model_runtime` so pre-alignment compatibility shells do not gain noisy fallback cards.
- 2026-04-07: For Assistant chat, keep model-runtime metadata attached to each AI reply instead of a global drawer-level state so the natural-language explanation and the runtime snapshot stay aligned to the same backend response.
- 2026-04-07: Keep the dashboard-level `AiAdvisor` summary card on a compact runtime strip instead of reusing the full `AdvisorModelRuntimePanel`, so the top hero card stays scrollable and the text summary remains readable.
- 2026-04-07: Only successful live Open-Meteo payloads populate the weather cache; cached reuse and synthetic fallback stay transient and must surface explicit provider/fallback labels in the weather panel instead of masquerading as live weather.
