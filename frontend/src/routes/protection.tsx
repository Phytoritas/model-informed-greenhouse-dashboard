import SectionRouteFrame, { type SectionRouteFrameProps } from './SectionRouteFrame';

export default function ProtectionRoute(props: SectionRouteFrameProps) {
  return <SectionRouteFrame {...props} />;
}
