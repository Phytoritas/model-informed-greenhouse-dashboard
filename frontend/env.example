# Copy this file to `frontend/.env` (Vite will load it automatically)
#
# Only one setting is required:
VITE_BACKEND_ORIGIN=http://localhost:8000
#
# Optional overrides (advanced):
# VITE_API_URL=http://localhost:8000/api
# VITE_WS_URL=ws://localhost:8000/ws/sim

